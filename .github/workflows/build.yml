name: Build & Release

on:
  push:
    tags:
      - 'v*'

jobs:

  # ─── Linux AppImage ────────────────────────────────────────────────────────
  build-linux:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            libappindicator3-dev \
            librsvg2-dev \
            patchelf \
            libssl-dev \
            pkg-config \
            libdbus-1-dev \
            libglib2.0-dev

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Rust
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: src-tauri

      - name: Set version from tag
        run: |
          VERSION=${GITHUB_REF_NAME#v}
          sed -i "s/\"version\": \".*\"/\"version\": \"$VERSION\"/" src-tauri/tauri.conf.json

      - name: Install npm dependencies
        run: npm install

      - name: Build Linux AppImage
        run: npm run tauri build -- --bundles appimage --verbose
        env:
          TAURI_SIGNING_PRIVATE_KEY: ""
          CARGO_TERM_COLOR: always

      - name: Rename artifact
        run: |
          find src-tauri/target/release/bundle/appimage -name "*.AppImage" \
            -exec cp {} venoapp-bridge.AppImage \;

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: linux-appimage
          path: venoapp-bridge.AppImage

  # ─── Windows NSIS installer ───────────────────────────────────────────────
  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Rust
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: src-tauri

      - name: Set version from tag
        shell: bash
        run: |
          VERSION=${GITHUB_REF_NAME#v}
          sed -i "s/\"version\": \".*\"/\"version\": \"$VERSION\"/" src-tauri/tauri.conf.json

      - name: Install npm dependencies
        run: npm install

      - name: Build Windows installer
        run: npm run tauri build -- --bundles nsis --verbose
        env:
          TAURI_SIGNING_PRIVATE_KEY: ""
          CARGO_TERM_COLOR: always

      - name: Rename artifact
        shell: bash
        run: |
          find src-tauri/target/release/bundle/nsis -name "*.exe" \
            -exec cp {} venoapp-bridge.exe \;

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: windows-installer
          path: venoapp-bridge.exe

  # ─── Create GitHub Release ────────────────────────────────────────────────
  release:
    needs: [build-linux, build-windows]
    runs-on: ubuntu-22.04
    permissions:
      contents: write
    steps:
      - name: Download Linux artifact
        uses: actions/download-artifact@v4
        with:
          name: linux-appimage

      - name: Download Windows artifact
        uses: actions/download-artifact@v4
        with:
          name: windows-installer

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          name: VenoApp Bridge ${{ github.ref_name }}
          body: |
            ## VenoApp Bridge ${{ github.ref_name }}

            ### Install on your venue device

            | Platform | Download |
            |----------|----------|
            | Windows PC | `venoapp-bridge.exe` |
            | Linux PC / Raspberry Pi | `venoapp-bridge.AppImage` |

            **Windows:** Run the installer (.exe). If SmartScreen warns you, click "More info" → "Run anyway".
            **Linux / Raspberry Pi:** Download the AppImage, make it executable (`chmod +x`), then run it.

            After installing, open VenoApp Bridge once to enter your restaurant code and printer IP. It will run silently in the background from then on.
          files: |
            venoapp-bridge.exe
            venoapp-bridge.AppImage
          draft: false
          prerelease: false
